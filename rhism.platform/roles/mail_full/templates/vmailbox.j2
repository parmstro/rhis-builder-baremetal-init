{# Managed by Ansible — role mail_full. Do not edit by hand. #}
# Postfix virtual mailbox map — managed by Ansible (mail_full role)
# Do not edit manually; changes will be overwritten.
# Format: user@domain  domain/user/Maildir/
{% for user in mail_virtual_users | default([]) %}
{% set addr = user.email | default(user.name) | default(user) %}
{{ addr }}    {{ addr.split('@')[1] }}/{{ addr.split('@')[0] }}/Maildir/
{% endfor %}
