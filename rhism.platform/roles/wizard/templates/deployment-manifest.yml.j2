---
# ACME Infrastructure Deployment Manifest
# Profile: {{ wizard_profile | default('custom') }}
# Generated by infra_wizard — edit with care, validate with wizard_action=validate

profile: {{ wizard_profile | default('custom') }}

components:
{{ wizard_components | to_nice_yaml(indent=2) | indent(2, first=true) }}
network:
{{ wizard_network | to_nice_yaml(indent=2) | indent(2, first=true) }}
sizing:
{{ wizard_sizing | to_nice_yaml(indent=2) | indent(2, first=true) }}
credentials:
{{ wizard_credentials | to_nice_yaml(indent=2) | indent(2, first=true) }}
licensing:
{{ wizard_licensing | default({}) | to_nice_yaml(indent=2) | indent(2, first=true) }}
