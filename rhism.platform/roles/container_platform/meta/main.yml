---
galaxy_info:
  author: rhism
  description: >-
    Container platform deployment — K3s, RKE2, OKD, and OCP.
    Installs and configures Kubernetes/OpenShift clusters with optional
    storage (local-path, NFS, Longhorn, OCS), registry, and ingress setup.
  license: Apache-2.0
  min_ansible_version: "2.14"
  platforms:
    - name: EL
      versions:
        - "8"
        - "9"
  galaxy_tags:
    - kubernetes
    - openshift
    - k3s
    - rke2
    - containers
    - platform_builder
dependencies: []
