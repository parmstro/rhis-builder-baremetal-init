---
galaxy_info:
  namespace: rhism
  role_name: k3s
  author: rhism
  description: >-
    Install and manage a K3s lightweight Kubernetes cluster (server/agent, config, service,
    node join/eviction, upgrade, etcd backup/restore, teardown). Standalone-first product
    role, also selectable by the container_platform dispatcher (cp_type: k3s).
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions:
        - "9"
  galaxy_tags:
    - kubernetes
    - k3s
    - k8s
    - cluster
    - rancher
dependencies: []
